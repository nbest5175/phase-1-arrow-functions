const divide = (number1, number2) => number1 / number2
divide(2000,100);
const square = (x) => x * x;
square(2)
const add = (a, b) => {
    console.log(`Adding ${a}`);
    console.log(`Adding ${b}`);
    return a + b;
  }
  add(3, 4);